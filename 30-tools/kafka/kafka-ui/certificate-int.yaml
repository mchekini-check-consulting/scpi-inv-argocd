apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kafka-ui-int-tls
spec:
  secretName: kafka-ui-int-tls
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  dnsNames:
    - int.kafka-ui.check-consulting.net
  usages:
    - digital signature
    - key encipherment
