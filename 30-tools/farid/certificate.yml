apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: farid-tls
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: lets-encrypt
  secretName: farid-tls
  dnsNames:
    - farid.check-consulting.net
  usages:
    - digital signature
    - key encipherment